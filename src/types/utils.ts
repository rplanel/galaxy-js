/**
 * HashFunctionNames
 * @description Hash function names that can be used to generate checksums for datasets.
 * @enum {string}
 */
export type HashFunctionNames = 'MD5' | 'SHA-1' | 'SHA-256' | 'SHA-512'
